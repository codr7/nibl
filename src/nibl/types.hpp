#ifndef NIBL_TYPES_HPP
#define NIBL_TYPES_HPP

#include <cstdint>
#include <string>

namespace nibl {
  using namespace std;

  using Char = char;
  using Int = int64_t;
  using PC = size_t;
  using Str = string;
  using Tag = size_t;
}

#endif
